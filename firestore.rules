rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita para todas as coleções do sistema
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
